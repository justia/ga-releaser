import OriginalConventionalChangelog from '@release-it/conventional-changelog';
import { type IncrementBase } from 'release-it';
declare class ConventionalChangelog extends OriginalConventionalChangelog {
    generateChangelog(options?: Readonly<Record<string, unknown>>): Promise<string>;
    getIncrementedVersion({ increment, latestVersion, isPreRelease, preReleaseId }: Readonly<IncrementBase>): Promise<string>;
}
export = ConventionalChangelog;
