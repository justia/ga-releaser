import { Plugin } from 'release-it';
import { type GitHub } from '@actions/github/lib/utils';
import { type RestEndpointMethodTypes } from '@octokit/plugin-rest-endpoint-methods';
declare class GitHubPlugin extends Plugin {
    octokit: InstanceType<typeof GitHub>;
    isEnabled: boolean;
    owner: string;
    repo: string;
    init(): void;
    registerPrompts(): void;
    afterRelease(): Promise<unknown>;
    setRepoInformation(repo: {
        readonly owner: string;
        readonly project: string;
    }): void;
    createPullRequest(version: string, body: string): Promise<RestEndpointMethodTypes['pulls']['create']['response']>;
    mergePullRequest(number: number): Promise<RestEndpointMethodTypes['pulls']['merge']['response']>;
    octokitMergePullRequest(octokit: Readonly<InstanceType<typeof GitHub>>, number: number): Promise<RestEndpointMethodTypes['pulls']['merge']['response']>;
}
export = GitHubPlugin;
