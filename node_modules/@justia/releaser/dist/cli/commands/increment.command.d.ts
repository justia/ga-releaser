import { IReleaseItOptions } from 'release-it';
declare const _default: (increment: string, config: IReleaseItOptions) => boolean;
/**
 * Command to set an specific version or bump
 */
export default _default;
