import { IReleaseItOptions } from 'release-it';
declare const _default: (format: string, config: IReleaseItOptions) => boolean;
/**
 * Command to set the calendar versioning
 */
export default _default;
